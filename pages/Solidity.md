- Créer une référence au contrat situé à l'adresse `0x123...` à travers l'interface. Le contrat à l'adresse `0x123...` doit implémenter l'interface.
	- ```solidity
	  MyInterface myContract = MyInterface(0x123...)
	  ```
-